export function googleAgentCliFrame() {
  return {
    provider: "Google Agents CLI / ADK",
    mode: "architecture-ready",
    purpose:
      "Agent lifecycle frame: build, evaluate, and deploy the Mouthpiece web-errand agent using Google's ADK/Agents CLI stack.",
    localPlan: [
      "Define Mouthpiece as an ADK agent with tools for Nimble extraction, Senso context, and ClickHouse logging.",
      "Evaluate each errand scenario as an agent test case.",
      "Use Agents CLI/ADK as the Google/DeepMind sponsor-aligned development path.",
    ],
  };
}
